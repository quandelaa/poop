[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "file-organizer"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
"rich"
]

[project.scripts]
poop = "poop_project.main:main"
